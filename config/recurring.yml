# examples:
#   periodic_cleanup:
#     class: CleanSoftDeletedRecordsJob
#     queue: background
#     args: [ 1000, { batch_size: 500 } ]
#     schedule: every hour
#   periodic_cleanup_with_command:
#     command: "SoftDeletedRecord.due.delete_all"
#     priority: 2
#     schedule: at 5am every day

production:
  watchdog_daily_snapshot:
    class: WatchdogJob
    schedule: "0 0 * * *" # Tous les jours à minuit
development:
  watchdog_daily_snapshot:
    class: WatchdogJob
    schedule: "every hour" # Toutes les heures en dev pour les tests
